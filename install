#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

brew trust "andrewvos/tap"
brew trust "atlassian-labs/acli"
brew trust "avivsinai/tap"
brew trust "hashicorp/tap"
brew trust "oven-sh/bun"

brew bundle install

function add-line-to-file() {
    FILE="$1"
    LINE="$2"

    grep -qxF "$LINE" "$FILE" || echo "$LINE" >>"$FILE"
}

add-line-to-file ~/.zshrc 'export PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"'
asdf plugin add nodejs https://github.com/asdf-vm/asdf-nodejs.git || :
asdf plugin add golang https://github.com/asdf-community/asdf-golang.git || :
asdf plugin add ruby || :
asdf install nodejs 22.4.0
asdf install golang 1.19

add-line-to-file ~/.zshrc 'eval "$(starship init zsh)"'
add-line-to-file ~/.zprofile 'export PATH=$PATH:/Applications/Postgres.app/Contents/Versions/latest/bin'

mkdir -p ~/.config
mkdir -p ~/.gnupg
chmod 700 ~/.gnupg

stow git
stow starship
stow ghostty
stow gnupg